# CORRECT - specify Python version and network access
[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[[services]]
name = "bot"
port = 8080  # Railway needs a port exposed
